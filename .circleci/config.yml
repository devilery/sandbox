version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:10

    working_directory: ~/repo

    steps:
      - checkout
      
      - run:
          name: Build ...
          command: sleep 15 && echo ok

      - deploy:
          name: Deploying ...
          command: echo deploying stuff
          
  build2:
    docker:
      - image: circleci/node:10

    working_directory: ~/repo

    steps:
      - checkout
      
      - run:
          name: Build ...
          command: sleep 30 && echo okkk

      - deploy:
          name: Deploying ...
          command: echo deploying stuff

  fakedeploy:
    docker:
      - image: circleci/node:10

    working_directory: ~/repo

    steps:
      - checkout
      - run: echo uploading assets into cloud gg  .... && sleep 5

workflows:
  version: 2
  ourpipeline2:
    jobs:
      - build
      - approve-long:
          type: approval
          requires:
            - build
      - approve-chained:
          type: approval
          requires:
            - approve-long
      - build2:
          requires:
            - build
            - approve-long
      - fakedeploy:
          requires:
            - build
            - build2
